# petclinic# petclinic-app
#### Kubernetes helm deployment of petclinic app
```
1) git clone the repo
2) cd petclinic-kube-helm-deployment/helm
3) helm package petclinic
4) helm install petclinic petclinic-0.1.0.tgz
5) helm list 
```
